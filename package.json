{
  "name": "settlers-of-lechia",
  "version": "0.1.0",
  "description": "A Catan-inspired multiplayer game with Polish/Lechia theme",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client --include=dev",
    "build": "npm run build --prefix client",
    "start": "NODE_ENV=production node server/src/index.js",
    "dev:server": "node server/src/index.js",
    "dev:client": "npm run dev --prefix client"
  },
  "engines": {
    "node": ">=18"
  }
}
